All tracked issues have been addressed in this repo.

What I changed most recently:

- Improved the `NewTabPage` favourites layout so chips are accessible on desktop and mobile:
	- On narrow screens the favourites remain a horizontal scroll row with a visible scrollbar.
	- On wider screens chips wrap onto multiple lines so all favourites are visible and selectable.

If you'd like, I can now run more manual QA flows, add visual tests, or open the website repo to verify any remaining external fixes.
