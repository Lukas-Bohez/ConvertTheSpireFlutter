# Microsoft Store Publishing

Microsoft signs the MSIX for you, so the upload artifact is an unsigned package.

## Build

```powershell
.\scripts\build_msix.ps1
```

## Notes

* Package name used by the app: `OrokaConner.ConvertTheSpireReborn`
* Store packaging guide lives here because it changes less often than the root README.
