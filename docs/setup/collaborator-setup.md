# Collaborator Setup

Use this guide when adding new contributors.

## Quick Start

1. Clone the repository.
2. Run `flutter pub get`.
3. Verify with `flutter analyze lib`.
4. Use the platform-specific `flutter run` command for local testing.

## Repository Access

Add collaborators through GitHub repository settings, then point them at this docs folder for build and release instructions.
